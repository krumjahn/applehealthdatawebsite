{
  "name": "cloudflare-worker",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "check": "npm test && WRANGLER_LOG_PATH=.wrangler/check.log wrangler deploy --dry-run --outdir ../../.scratch/applehealthdata-worker-dry-run",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@peculiar/x509": "^2.1.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
